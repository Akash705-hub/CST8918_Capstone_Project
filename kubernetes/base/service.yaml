apiVersion: v1
kind: Service
metadata:
  name: remix-weather-app
  namespace: default
  labels:
    app: remix-weather-app
spec:
  type: LoadBalancer
  selector:
    app: remix-weather-app
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  sessionAffinity: None
